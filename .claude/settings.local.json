{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "prompt",
            "prompt": "请检查是否需要同步项目文件到远程服务器：\n\n1. 首先检查 .vscode/sftp.json 配置文件是否存在\n2. 如果配置文件已存在，询问用户是否要执行同步操作\n3. 如果配置文件不存在，询问用户是否要配置 SFTP 同步\n4. 如果用户同意，调用 /sync 命令进行配置和同步\n\n如果用户拒绝或跳过，则不执行任何操作。"
          }
        ]
      }
    ]
  }
}